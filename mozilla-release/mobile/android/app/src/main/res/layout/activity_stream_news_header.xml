<?xml version="1.0" encoding="utf-8"?>
<!--Copyright © Cliqz 2018-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <org.mozilla.gecko.widget.themed.ThemedTextView
        android:id="@+id/news_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="start"
        android:padding="@dimen/news_title_padding"
        android:text="@string/activity_stream_top_news"
        android:textColor="@color/cliqz_start_tab_font_color"
        android:textSize="14sp"
        android:textStyle="bold"
        tools:text="News" />

    <org.mozilla.gecko.widget.themed.ThemedTextView
        android:id="@+id/news_display_toggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="end"
        android:padding="@dimen/news_title_padding"
        android:text="@string/activity_stream_top_news"
        android:textColor="@color/cliqz_start_tab_font_color"
        android:textSize="14sp"
        tools:text="Show" />

</LinearLayout>